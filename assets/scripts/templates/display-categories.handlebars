<h2 class="score">
  Welcome, {{game.user.name}}! <br />
  You have: ${{game.user.score}}
</h2>

{{#each game.categories}}
  <div class="category">
    <h3 class="category-title" id="{{this.id}}">{{name}}</h3>
    {{> display-question-values}}
    </div>
  </div>
{{/each}}
